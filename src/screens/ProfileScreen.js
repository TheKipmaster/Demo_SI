import React from 'react';
import { Card, CardItem, Button } from '../components/common';

const LoginScreen = () => {
  return (
    <Card>
      <CardItem>
        <Button>Botão</Button>
      </CardItem>
    </Card>
  );
}

export default LoginScreen;